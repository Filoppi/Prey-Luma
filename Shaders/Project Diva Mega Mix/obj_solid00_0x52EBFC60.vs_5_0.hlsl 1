// ---- Created with 3Dmigoto v1.3.16 on Tue Sep 02 15:46:07 2025

cbuffer Shader : register(b0)
{
  uint4 g_shader_flags : packoffset(c0);
}

cbuffer Scene : register(b1)
{
  float4 g_irradiance_r_transforms[4] : packoffset(c0);
  float4 g_irradiance_g_transforms[4] : packoffset(c4);
  float4 g_irradiance_b_transforms[4] : packoffset(c8);
  float4 g_light_env_stage_diffuse : packoffset(c12);
  float4 g_light_env_stage_specular : packoffset(c13);
  float4 g_light_env_chara_diffuse : packoffset(c14);
  float4 g_light_env_chara_ambient : packoffset(c15);
  float4 g_light_env_chara_specular : packoffset(c16);
  float4 g_light_env_reflect_diffuse : packoffset(c17);
  float4 g_light_env_reflect_ambient : packoffset(c18);
  float4 g_light_env_reflect_specular : packoffset(c19);
  float4 g_light_env_proj_diffuse : packoffset(c20);
  float4 g_light_env_proj_specular : packoffset(c21);
  float4 g_light_env_proj_position : packoffset(c22);
  float4 g_light_stage_dir : packoffset(c23);
  float4 g_light_stage_diff : packoffset(c24);
  float4 g_light_stage_spec : packoffset(c25);
  float4 g_light_chara_dir : packoffset(c26);
  float4 g_light_chara_spec : packoffset(c27);
  float4 g_light_chara_luce : packoffset(c28);
  float4 g_light_chara_back : packoffset(c29);
  float4 g_light_face_diff : packoffset(c30);
  float4 g_chara_color0 : packoffset(c31);
  float4 g_chara_color1 : packoffset(c32);
  float4 g_chara_f_dir : packoffset(c33);
  float4 g_chara_f_ambient : packoffset(c34);
  float4 g_chara_f_diffuse : packoffset(c35);
  float4 g_chara_tc_param : packoffset(c36);
  float4 g_fog_depth_color : packoffset(c37);
  float4 g_fog_height_params : packoffset(c38);
  float4 g_fog_height_color : packoffset(c39);
  float4 g_fog_bump_params : packoffset(c40);
  float4 g_fog_state_params : packoffset(c41);
  float4 g_normal_tangent_transforms[3] : packoffset(c42);
  float4 g_esm_param : packoffset(c45);
  float4 g_self_shadow_receivers[6] : packoffset(c46);
  float4 g_shadow_ambient : packoffset(c52);
  float4 g_shadow_ambient1 : packoffset(c53);
  float4 g_framebuffer_size : packoffset(c54);
  float4 g_light_reflect_dir : packoffset(c55);
  float4 g_clip_plane : packoffset(c56);
  float4 g_npr_cloth_spec_color : packoffset(c57);
  float4 g_view[3] : packoffset(c58);
  float4 g_view_inverse[3] : packoffset(c61);
  float4 g_projection_view[4] : packoffset(c64);
  float4 g_view_position : packoffset(c68);
  float4 g_light_projection[4] : packoffset(c69);
  float4 g_light_projection_depth[4] : packoffset(c73);
  float4 g_foward_z_projection_row2 : packoffset(c77);
}

cbuffer Batch : register(b2)
{
  float4 g_transforms[4] : packoffset(c0);
  float4 g_worlds[3] : packoffset(c4);
  float4 g_worlds_invtrans[3] : packoffset(c7);
  float4 g_worldview[3] : packoffset(c10);
  float4 g_worldview_inverse[3] : packoffset(c13);
  float4 g_joint[3] : packoffset(c16);
  float4 g_joint_inverse[3] : packoffset(c19);
  float4 g_texcoord_transforms[4] : packoffset(c22);
  float4 g_blend_color : packoffset(c26);
  float4 g_offset_color : packoffset(c27);
  float4 g_material_state_diffuse : packoffset(c28);
  float4 g_material_state_ambient : packoffset(c29);
  float4 g_material_state_emission : packoffset(c30);
  float4 g_material_state_shininess : packoffset(c31);
  float4 g_material_state_specular : packoffset(c32);
  float4 g_fresnel_coefficients : packoffset(c33);
  float4 g_texture_color_coefficients : packoffset(c34);
  float4 g_texture_color_offset : packoffset(c35);
  float4 g_texture_specular_coefficients : packoffset(c36);
  float4 g_texture_specular_offset : packoffset(c37);
  float4 g_shininess : packoffset(c38);
  float4 g_max_alpha : packoffset(c39);
  float4 g_morph_weight : packoffset(c40);
  float4 g_sss_param : packoffset(c41);
  float4 g_bump_depth : packoffset(c42);
  float4 g_intensity : packoffset(c43);
  float4 g_reflect_uv_scale : packoffset(c44);
  float4 g_shadow_position[2] : packoffset(c45);
  float4 g_shadow_direction[2] : packoffset(c47);
  float4 g_shadow_param : packoffset(c49);
  uint4 g_skip_flags : packoffset(c50);
}

cbuffer NPRParam : register(b4)
{
  uint4 g_npr_flags : packoffset(c0);
  float4 g_npr_param[8] : packoffset(c1);
  uint4 g_npr_dbg_flags : packoffset(c9);
  float4 g_npr_dbg_param[4] : packoffset(c10);
}

SamplerState g_effect_sampler_s : register(s14);
Texture2D<float4> g_effect_texture : register(t14);


// 3Dmigoto declarations
#define cmp -

#include "./Includes/Common.hlsl"

void main(
  float4 v0 : POSITION0,
  float3 v1 : NORMAL0,
  float4 v2 : COLOR0,
  float4 v3 : TEXCOORD0,
  float4 v4 : TEXCOORD1,
  float4 v5 : TEXCOORD4,
  out float4 o0 : SV_POSITION0,
  out float3 o1 : NORMAL0,
  out float3 o2 : TANGENT0,
  out float3 o3 : BINORMAL0,
  out float4 o4 : TEXCOORD0,
  out float4 o5 : TEXCOORD1,
  out float4 o6 : TEXCOORD2,
  out float4 o7 : TEXCOORD3,
  out float4 o8 : COLOR0,
  out float4 o9 : COLOR1,
  out float4 o10 : COLOR2,
  out float4 o11 : COLOR3)
{
  float4 r0,r1,r2,r3,r4,r5,r6,r7,r8;
  uint4 bitmask, uiDest;
  float4 fDest;

  o0.x = dot(g_transforms[0].xyzw, v0.xyzw);
  o0.y = dot(g_transforms[1].xyzw, v0.xyzw);
  o0.z = dot(g_transforms[2].xyzw, v0.xyzw);
  o0.w = dot(g_transforms[3].xyzw, v0.xyzw);
  r0.x = dot(g_worlds_invtrans[0].xyz, v1.xyz);
  r0.y = dot(g_worlds_invtrans[1].xyz, v1.xyz);
  r0.z = dot(g_worlds_invtrans[2].xyz, v1.xyz);
  r1.x = dot(g_worlds_invtrans[0].xyz, v5.xyz);
  r1.y = dot(g_worlds_invtrans[1].xyz, v5.xyz);
  r1.z = dot(g_worlds_invtrans[2].xyz, v5.xyz);
  r0.w = dot(r0.xyz, r0.xyz);
  r0.w = rsqrt(r0.w);
  r0.xyz = r0.xyz * r0.www;
  r1.w = dot(r1.xyz, r1.xyz);
  r1.w = rsqrt(r1.w);
  r1.xyz = r1.xyz * r1.www;
  r2.xyz = r1.yzx * r0.zxy;
  r2.xyz = r0.yzx * r1.zxy + -r2.xyz;
  o3.xyz = v5.www * r2.xyz;
  if (1 == 0) r2.x = 0; else if (1+8 < 32) {   r2.x = (uint)g_shader_flags.w << (32-(1 + 8)); r2.x = (uint)r2.x >> (32-1);  } else r2.x = (uint)g_shader_flags.w >> 8;
  if (1 == 0) r2.y = 0; else if (1+13 < 32) {   r2.y = (uint)g_shader_flags.w << (32-(1 + 13)); r2.y = (uint)r2.y >> (32-1);  } else r2.y = (uint)g_shader_flags.w >> 13;
  r1.w = cmp((int)r2.x != 1);
  r2.x = cmp(0 < (uint)g_npr_flags.x);
  if (1 == 0) r2.z = 0; else if (1+7 < 32) {   r2.z = (uint)g_npr_flags.y << (32-(1 + 7)); r2.z = (uint)r2.z >> (32-1);  } else r2.z = (uint)g_npr_flags.y >> 7;
  r2.z = cmp((int)r2.z != 0);
  r2.x = r2.z ? r2.x : 0;
  r2.z = (int)g_npr_flags.y & 1;
  r2.z = cmp((int)r2.z == 0);
  r2.x = r2.z ? r2.x : 0;
  r1.w = (int)r1.w | (int)r2.x;
  r0.w = 1;
  r3.x = dot(g_irradiance_r_transforms[0].xyzw, r0.xyzw);
  r3.y = dot(g_irradiance_r_transforms[1].xyzw, r0.xyzw);
  r3.z = dot(g_irradiance_r_transforms[2].xyzw, r0.xyzw);
  r3.w = dot(g_irradiance_r_transforms[3].xyzw, r0.xyzw);
  o10.x = dot(r3.xyzw, r0.xyzw);
  r3.x = dot(g_irradiance_g_transforms[0].xyzw, r0.xyzw);
  r3.y = dot(g_irradiance_g_transforms[1].xyzw, r0.xyzw);
  r3.z = dot(g_irradiance_g_transforms[2].xyzw, r0.xyzw);
  r3.w = dot(g_irradiance_g_transforms[3].xyzw, r0.xyzw);
  o10.y = dot(r3.xyzw, r0.xyzw);
  r3.x = dot(g_irradiance_b_transforms[0].xyzw, r0.xyzw);
  r3.y = dot(g_irradiance_b_transforms[1].xyzw, r0.xyzw);
  r3.z = dot(g_irradiance_b_transforms[2].xyzw, r0.xyzw);
  r3.w = dot(g_irradiance_b_transforms[3].xyzw, r0.xyzw);
  o10.z = dot(r3.xyzw, r0.xyzw);
  o10.w = r1.w ? 1.000000 : 0;
  r3.xy = v3.xy;
  r3.zw = float2(1,1);
  o4.x = dot(g_texcoord_transforms[0].xyw, r3.xyz);
  o4.y = dot(g_texcoord_transforms[1].xyw, r3.xyz);
  r3.xy = v4.xy;
  r3.z = 1;
  o4.z = dot(g_texcoord_transforms[2].xyw, r3.xyz);
  o4.w = dot(g_texcoord_transforms[3].xyw, r3.xyz);
  r3.x = dot(g_worlds[0].xyzw, v0.xyzw);
  r3.y = dot(g_worlds[1].xyzw, v0.xyzw);
  r3.z = dot(g_worlds[2].xyzw, v0.xyzw);
  r2.xzw = g_view_position.xyz + -r3.xyz;
  r0.w = dot(r2.xzw, r2.xzw);
  r0.w = rsqrt(r0.w);
  r4.xyz = r2.xzw * r0.www;
  r5.x = dot(g_self_shadow_receivers[0].xyzw, r3.xyzw);
  r5.y = dot(g_self_shadow_receivers[1].xyzw, r3.xyzw);
  r6.z = dot(g_self_shadow_receivers[2].xyzw, r3.xyzw);
  r7.x = dot(g_self_shadow_receivers[3].xyzw, r3.xyzw);
  r7.y = dot(g_self_shadow_receivers[4].xyzw, r3.xyzw);
  r8.z = dot(g_self_shadow_receivers[5].xyzw, r3.xyzw);
  r6.xy = g_shadow_param.yy * r5.xy;
  r8.xy = g_shadow_param.yy * r7.xy;
  o6.xyz = r2.yyy ? r6.xyz : float3(0,0,0);
  o7.xyz = r2.yyy ? r8.xyz : float3(0,0,0);
  r0.w = dot(r0.xyz, r4.xyz);
  r0.w = saturate(1 + -r0.w);
  r1.w = r0.w * r0.w;
  r1.w = r1.w * r1.w;
  r0.w = r1.w * r0.w;
  r1.w = g_fresnel_coefficients.x * r0.w;
  r4.xyz = r0.www * g_fresnel_coefficients.xxx + g_fresnel_coefficients.yyy;
  r4.w = r1.w * 10 + 1;
  o9.xyzw = g_material_state_specular.xyzw * r4.xyzw;
  r4.xyzw = g_light_env_stage_diffuse.xyzw * v2.xyzw;
  o8.xyzw = g_blend_color.xyzw * r4.xyzw;
  if (1 == 0) r0.w = 0; else if (1+13 < 32) {   r0.w = (uint)g_skip_flags.x << (32-(1 + 13)); r0.w = (uint)r0.w >> (32-1);  } else r0.w = (uint)g_skip_flags.x >> 13;
  r4.xyz = -g_shadow_position[0].xyz + r3.xyz;
  r1.w = dot(r4.xyz, g_shadow_direction[0].xyz);
  r2.y = dot(r4.xyz, r4.xyz);
  r1.w = -r1.w * r1.w + r2.y;
  r4.xyz = -g_shadow_position[1].xyz + r3.xyz;
  r2.y = dot(r4.xyz, g_shadow_direction[1].xyz);
  r3.w = dot(r4.xyz, r4.xyz);
  r2.y = -r2.y * r2.y + r3.w;
  r1.w = cmp(g_shadow_param.x < r1.w);
  r2.y = cmp(g_shadow_param.x < r2.y);
  r1.w = r1.w ? r2.y : 0;
  r1.w = r1.w ? 0 : 1;
  o11.w = r0.w ? 1 : r1.w;
  r4.x = dot(g_worldview[2].xyzw, v0.xyzw);
  r4.y = 1;
  r0.w = dot(g_foward_z_projection_row2.zw, r4.xy);
  r1.w = (int)g_shader_flags.y & 3;
  r2.y = cmp((int)r1.w == 1);
  if (r2.y != 0) {
    // g_fog_state_params 0.9, -1.1, 26.2, 0.03663
    r2.y = -g_fog_state_params.y + r0.w;
    r2.y = saturate(g_fog_state_params.w * r2.y);
    o6.w = g_fog_state_params.x * r2.y;
  } else {
    r2.y = cmp((int)r1.w == 2);
    if (r2.y != 0) {
      r0.w = -g_fog_state_params.y + r0.w;
      r0.w = saturate(g_fog_state_params.w * r0.w);
      r0.w = g_fog_state_params.x * r0.w;
      r2.y = -g_fog_height_params.y + -r3.y;
      r2.y = saturate(g_fog_height_params.w * r2.y);
      r2.y = g_fog_height_params.x * r2.y;
      o6.w = max(r2.y, r0.w);
    } else {
      r0.w = cmp((int)r1.w == 3);
      if (r0.w != 0) {
        r3.xz = r3.xz * float2(0.0625,-0.0625) + float2(0.5,0.5);
        r0.w = g_effect_texture.SampleLevel(g_effect_sampler_s, r3.xz, 0).x;
        r1.w = -g_fog_height_params.y + -r3.y;
        r1.w = saturate(-r1.w * g_fog_height_params.w + 1);
        r0.w = r0.w * 3 + 1;
        r2.y = cmp(0 < r0.w);
        r1.w = log2(r1.w);
        r0.w = r1.w * r0.w;
        r0.w = exp2(r0.w);
        r0.w = 1 + -r0.w;
        r0.w = g_fog_height_params.x * r0.w;
        o6.w = r2.y ? r0.w : 0;
      } else {
        o6.w = 0;
      }
    }
  }
  o7.w = 0;
  o1.xyz = r0.xyz;
  o2.xyz = r1.xyz;
  o5.xyz = r2.xzw;
  return;
}