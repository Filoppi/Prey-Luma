// Borderlands 2 / The Pre-Sequel — emissive Scaleform HUD sprite (autosave icon), dgVoodoo 2.81.3 -> ps_4_0 (hash 0xAE61FB46).
// Same shader as the 2.87.3 variant (0xC84956AA) with an identical signature; blind-include its body (adds the
// saturate that stops the icon blowing to peak nits in HDR).
#include "UIEmissive_0xC84956AA.ps_5_0.hlsl"
