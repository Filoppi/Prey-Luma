Texture2D<float4> t0 : register(t0);

SamplerState s0_s : register(s0);

cbuffer cb0 : register(b0)
{
  float4 cb0[2];
}

#define cmp

void main(
  float4 v0 : SV_POSITION0,
  float2 v1 : TEXCOORD0,
  out float4 o0 : SV_TARGET0)
{
  float4 r0,r1,r2,r3,r4,r5,r6,r7;
  r0.xy = float2(1,1) / cb0[0].xy;
  r1.xyzw = t0.Sample(s0_s, v1.xy).xyzw;
  r0.w = dot(r1.xyz, float3(0.298999995,0.587000012,0.114));
  r0.w = sqrt(r0.w);
  r2.yz = saturate(v1.yx + r0.yx);
  r2.xw = saturate(v1.xy);
  r3.xyz = t0.Sample(s0_s, r2.xy).xyz;
  r3.x = dot(r3.xyz, float3(0.298999995,0.587000012,0.114));
  r3.x = sqrt(r3.x);
  r4.xy = saturate(v1.yx + -r0.yx);
  r4.zw = r2.xw;
  r3.yzw = t0.Sample(s0_s, r4.zx).xyz;
  r3.y = dot(r3.yzw, float3(0.298999995,0.587000012,0.114));
  r5.xyz = t0.Sample(s0_s, r4.yw).xyz;
  r3.z = dot(r5.xyz, float3(0.298999995,0.587000012,0.114));
  r5.xyz = t0.Sample(s0_s, r2.zw).xyz;
  r3.w = dot(r5.xyz, float3(0.298999995,0.587000012,0.114));
  r3.yzw = sqrt(r3.yzw);
  r4.zw = min(r3.xz, r3.yw);
  r4.z = min(r4.z, r4.w);
  r4.z = min(r4.z, r0.w);
  r4.w = max(r3.x, r3.y);
  r5.x = max(r3.z, r3.w);
  r4.w = max(r5.x, r4.w);
  r4.w = max(r4.w, r0.w);
  r4.z = r4.w + -r4.z;
  r4.w = 0.125 * r4.w;
  r4.w = max(0.0625, r4.w);
  r4.w = cmp(r4.z < r4.w);
  if (r4.w != 0) {
    o0.xyzw = r1.xyzw;
    return;
  }
  r1.xyz = t0.Sample(s0_s, r4.yx).xyz;
  r1.x = dot(r1.xyz, float3(0.298999995,0.587000012,0.114));
  r1.x = sqrt(r1.x);
  r1.yzw = t0.Sample(s0_s, r2.zy).xyz;
  r1.y = dot(r1.yzw, float3(0.298999995,0.587000012,0.114));
  r2.xw = r4.yx;
  r4.xyw = t0.Sample(s0_s, r2.xy).xyz;
  r1.z = dot(r4.xyw, float3(0.298999995,0.587000012,0.114));
  r2.xyz = t0.Sample(s0_s, r2.zw).xyz;
  r1.w = dot(r2.xyz, float3(0.298999995,0.587000012,0.114));
  r1.yzw = sqrt(r1.yzw);
  r2.xy = r3.xz + r3.yw;
  r2.zw = r1.zy + r1.xw;
  r4.x = r1.x + -r3.z;
  r4.y = r3.z + -r1.z;
  r4.x = r4.x + -r4.y;
  r4.y = r3.y + -r0.w;
  r4.w = -r3.x + r0.w;
  r4.y = r4.y + -r4.w;
  r4.x = abs(r4.y) * 2 + abs(r4.x);
  r4.y = r1.w + -r3.w;
  r4.w = r3.w + -r1.y;
  r4.y = r4.y + -r4.w;
  r4.x = r4.x + abs(r4.y);
  r1.yw = r1.yw + -r3.xy;
  r1.xz = r3.yx + -r1.xz;
  r1.xy = r1.wy + -r1.xz;
  r1.w = r3.w + -r0.w;
  r4.y = -r3.z + r0.w;
  r1.w = -r4.y + r1.w;
  r1.x = abs(r1.w) * 2 + abs(r1.x);
  r1.x = r1.x + abs(r1.y);
  r1.x = r4.x + -r1.x;
  r1.y = cmp(0 < r1.x);
  r1.x = cmp(r1.x < 0);
  r1.x = (int)-r1.y + (int)r1.x;
  r1.x = (int)r1.x;
  r1.x = max(0, r1.x);
  r1.x = cmp(0 != r1.x);
  r1.yz = r1.xx ? r3.yx : r3.zw;
  r1.w = r1.y + -r0.w;
  r3.x = r1.z + -r0.w;
  r3.y = -abs(r3.x) + abs(r1.w);
  r3.z = cmp(0 < r3.y);
  r3.y = cmp(r3.y < 0);
  r3.y = (int)-r3.z + (int)r3.y;
  r3.y = (int)r3.y;
  r3.y = max(0, r3.y);
  r1.w = max(abs(r3.x), abs(r1.w));
  r3.x = r1.x ? r0.y : r0.x;
  r3.y = cmp(0 != r3.y);
  r3.x = r3.y ? -r3.x : r3.x;
  r1.y = r3.y ? r1.y : r1.z;
  r1.y = r1.y + r0.w;
  r1.z = 0.5 * r1.y;
  r5.xy = r3.xx * float2(0.5,0.5) + v1.yx;
  r5.zw = v1.xy;
  r3.yz = r1.xx ? r5.zx : r5.yw;
  r0.z = 0;
  r0.xy = r1.xx ? r0.xz : r0.zy;
  r4.xy = r3.yz + -r0.xy;
  r3.yz = r3.yz + r0.xy;
  r5.xy = saturate(r4.xy);
  r5.xyz = t0.Sample(s0_s, r5.xy).xyz;
  r0.z = dot(r5.xyz, float3(0.298999995,0.587000012,0.114));
  r0.z = sqrt(r0.z);
  r5.xy = saturate(r3.yz);
  r5.xyz = t0.Sample(s0_s, r5.xy).xyz;
  r3.w = dot(r5.xyz, float3(0.298999995,0.587000012,0.114));
  r3.w = sqrt(r3.w);
  r5.x = -r1.y * 0.5 + r0.z;
  r5.y = -r1.y * 0.5 + r3.w;
  r5.zw = -r1.ww * float2(0.25,0.25) + abs(r5.xy);
  r6.xy = cmp(float2(0,0) < r5.zw);
  r5.zw = cmp(r5.zw < float2(0,0));
  r5.zw = (int2)-r6.xy + (int2)r5.zw;
  r5.zw = (int2)r5.zw;
  r5.zw = max(float2(0,0), r5.zw);
  r6.xy = float2(1,1) + -r5.zw;
  r6.xy = cmp(float2(0,0) != r6.xy);
  r7.xyzw = r6.xxyy ? r0.xyxy : 0;
  r4.xy = -r7.xy + r4.xy;
  r3.yz = r7.zw + r3.yz;
  r0.z = -r5.z * r5.w + 1;
  r0.z = cmp(0 != r0.z);
  if (r0.z != 0) {
    r5.zw = cmp(cb0[1].xx == float2(1,0));
    r0.z = r5.w ? 12 : 4;
    r0.z = r5.z ? 8 : r0.z;
    r5.zw = saturate(r4.xy);
    r7.xyz = t0.Sample(s0_s, r5.zw).xyz;
    r3.w = dot(r7.xyz, float3(0.298999995,0.587000012,0.114));
    r3.w = sqrt(r3.w);
    r3.w = -r1.y * 0.5 + r3.w;
    r5.x = r6.x ? r3.w : r5.x;
    r5.zw = saturate(r3.yz);
    r6.xzw = t0.Sample(s0_s, r5.zw).xyz;
    r3.w = dot(r6.xzw, float3(0.298999995,0.587000012,0.114));
    r3.w = sqrt(r3.w);
    r3.w = -r1.y * 0.5 + r3.w;
    r5.y = r6.y ? r3.w : r5.y;
    r5.zw = -r1.ww * float2(0.25,0.25) + abs(r5.xy);
    r6.xy = cmp(float2(0,0) < r5.zw);
    r5.zw = cmp(r5.zw < float2(0,0));
    r5.zw = (int2)-r6.xy + (int2)r5.zw;
    r5.zw = (int2)r5.zw;
    r5.zw = max(float2(0,0), r5.zw);
    r3.w = r5.z * r5.w;
    r6.xy = float2(1,1) + -r5.zw;
    r6.xy = cmp(float2(0,0) != r6.xy);
    r7.xyzw = r6.xxyy ? r0.xyxy : 0;
    r4.xy = -r7.xy + r4.xy;
    r3.yz = r7.zw + r3.yz;
    r3.w = cmp(0 != r3.w);
    r4.w = ~(int)r3.w;
    if (r3.w == 0) {
      r6.zw = saturate(r4.xy);
      r7.xyz = t0.Sample(s0_s, r6.zw).xyz;
      r3.w = dot(r7.xyz, float3(0.298999995,0.587000012,0.114));
      r3.w = sqrt(r3.w);
      r3.w = -r1.y * 0.5 + r3.w;
      r5.x = r6.x ? r3.w : r5.x;
      r6.xz = saturate(r3.yz);
      r6.xzw = t0.Sample(s0_s, r6.xz).xyz;
      r3.w = dot(r6.xzw, float3(0.298999995,0.587000012,0.114));
      r3.w = sqrt(r3.w);
      r3.w = -r1.y * 0.5 + r3.w;
      r5.y = r6.y ? r3.w : r5.y;
      r3.w = -r1.w * 0.25 + abs(r5.x);
      r6.x = cmp(0 < r3.w);
      r3.w = cmp(r3.w < 0);
      r3.w = (int)-r6.x + (int)r3.w;
      r3.w = (int)r3.w;
      r5.z = max(0, r3.w);
      r3.w = -r1.w * 0.25 + abs(r5.y);
      r6.x = cmp(0 < r3.w);
      r3.w = cmp(r3.w < 0);
      r3.w = (int)-r6.x + (int)r3.w;
      r3.w = (int)r3.w;
      r5.w = max(0, r3.w);
      r3.w = r5.z * r5.w;
      r6.xy = float2(1,1) + -r5.zw;
      r6.xy = cmp(float2(0,0) != r6.xy);
      r6.xyzw = r6.xxyy ? r0.xyxy : 0;
      r4.xy = -r6.xy + r4.xy;
      r3.yz = r6.zw + r3.yz;
      r3.w = cmp(0 != r3.w);
      r6.x = r3.w ? 3 : 4;
    } else {
      r6.x = 2;
      r3.w = -1;
    }
    r3.w = ~(int)r3.w;
    r3.w = (int)r3.w & (int)r4.w;
    r4.w = cmp((int)r6.x < (int)r0.z);
    r3.w = r4.w ? r3.w : 0;
    if (r3.w != 0) {
      r6.yz = float2(1,1) + -r5.zw;
      r6.yz = cmp(float2(0,0) != r6.yz);
      r7.xy = saturate(r4.xy);
      r7.xyz = t0.Sample(s0_s, r7.xy).xyz;
      r4.w = dot(r7.xyz, float3(0.298999995,0.587000012,0.114));
      r4.w = sqrt(r4.w);
      r4.w = -r1.y * 0.5 + r4.w;
      r5.x = r6.y ? r4.w : r5.x;
      r6.yw = saturate(r3.yz);
      r7.xyz = t0.Sample(s0_s, r6.yw).xyz;
      r4.w = dot(r7.xyz, float3(0.298999995,0.587000012,0.114));
      r4.w = sqrt(r4.w);
      r4.w = -r1.y * 0.5 + r4.w;
      r5.y = r6.z ? r4.w : r5.y;
      r6.yz = -r1.ww * float2(0.25,0.25) + abs(r5.xy);
      r7.xy = cmp(float2(0,0) < r6.yz);
      r6.yz = cmp(r6.yz < float2(0,0));
      r6.yz = (int2)-r7.xy + (int2)r6.yz;
      r6.yz = (int2)r6.yz;
      r5.zw = max(float2(0,0), r6.yz);
      r4.w = r5.z * r5.w;
      r6.yz = float2(1,1) + -r5.zw;
      r6.yz = cmp(float2(0,0) != r6.yz);
      r7.xyzw = r6.yyzz ? r0.xyxy : 0;
      r4.xy = -r7.xy + r4.xy;
      r3.yz = r7.zw + r3.yz;
      r4.w = cmp(0 != r4.w);
      r6.x = r4.w ? 4 : 5;
    } else {
      r4.w = -1;
    }
    r4.w = ~(int)r4.w;
    r3.w = (int)r3.w & (int)r4.w;
    r4.w = cmp((int)r6.x < (int)r0.z);
    r3.w = r4.w ? r3.w : 0;
    if (r3.w != 0) {
      r6.yz = float2(1,1) + -r5.zw;
      r6.yz = cmp(float2(0,0) != r6.yz);
      r7.xy = saturate(r4.xy);
      r7.xyz = t0.Sample(s0_s, r7.xy).xyz;
      r4.w = dot(r7.xyz, float3(0.298999995,0.587000012,0.114));
      r4.w = sqrt(r4.w);
      r4.w = -r1.y * 0.5 + r4.w;
      r5.x = r6.y ? r4.w : r5.x;
      r6.yw = saturate(r3.yz);
      r7.xyz = t0.Sample(s0_s, r6.yw).xyz;
      r4.w = dot(r7.xyz, float3(0.298999995,0.587000012,0.114));
      r4.w = sqrt(r4.w);
      r4.w = -r1.y * 0.5 + r4.w;
      r5.y = r6.z ? r4.w : r5.y;
      r6.yz = -r1.ww * float2(0.25,0.25) + abs(r5.xy);
      r7.xy = cmp(float2(0,0) < r6.yz);
      r6.yz = cmp(r6.yz < float2(0,0));
      r6.yz = (int2)-r7.xy + (int2)r6.yz;
      r6.yz = (int2)r6.yz;
      r5.zw = max(float2(0,0), r6.yz);
      r4.w = r5.z * r5.w;
      r6.yz = float2(1,1) + -r5.zw;
      r6.yz = cmp(float2(0,0) != r6.yz);
      r7.xyzw = r6.yyzz ? r0.xyxy : 0;
      r4.xy = -r7.xy + r4.xy;
      r3.yz = r7.zw + r3.yz;
      r4.w = cmp(0 != r4.w);
      r6.x = r4.w ? 5 : 6;
    } else {
      r4.w = -1;
    }
    r4.w = ~(int)r4.w;
    r3.w = (int)r3.w & (int)r4.w;
    r4.w = cmp((int)r6.x < (int)r0.z);
    r3.w = r4.w ? r3.w : 0;
    if (r3.w != 0) {
      r6.yz = float2(1,1) + -r5.zw;
      r6.yz = cmp(float2(0,0) != r6.yz);
      r7.xy = saturate(r4.xy);
      r7.xyz = t0.Sample(s0_s, r7.xy).xyz;
      r4.w = dot(r7.xyz, float3(0.298999995,0.587000012,0.114));
      r4.w = sqrt(r4.w);
      r4.w = -r1.y * 0.5 + r4.w;
      r5.x = r6.y ? r4.w : r5.x;
      r6.yw = saturate(r3.yz);
      r7.xyz = t0.Sample(s0_s, r6.yw).xyz;
      r4.w = dot(r7.xyz, float3(0.298999995,0.587000012,0.114));
      r4.w = sqrt(r4.w);
      r4.w = -r1.y * 0.5 + r4.w;
      r5.y = r6.z ? r4.w : r5.y;
      r6.yz = -r1.ww * float2(0.25,0.25) + abs(r5.xy);
      r7.xy = cmp(float2(0,0) < r6.yz);
      r6.yz = cmp(r6.yz < float2(0,0));
      r6.yz = (int2)-r7.xy + (int2)r6.yz;
      r6.yz = (int2)r6.yz;
      r5.zw = max(float2(0,0), r6.yz);
      r4.w = r5.z * r5.w;
      r6.yz = float2(1,1) + -r5.zw;
      r6.yz = cmp(float2(0,0) != r6.yz);
      r7.xyzw = r6.yyzz ? r0.xyxy : 0;
      r4.xy = -r7.xy + r4.xy;
      r3.yz = r7.zw + r3.yz;
      r4.w = cmp(0 != r4.w);
      r6.x = r4.w ? 6 : 7;
    } else {
      r4.w = -1;
    }
    r4.w = ~(int)r4.w;
    r3.w = (int)r3.w & (int)r4.w;
    r4.w = cmp((int)r6.x < (int)r0.z);
    r3.w = r4.w ? r3.w : 0;
    if (r3.w != 0) {
      r6.yz = float2(1,1) + -r5.zw;
      r6.yz = cmp(float2(0,0) != r6.yz);
      r7.xy = saturate(r4.xy);
      r7.xyz = t0.Sample(s0_s, r7.xy).xyz;
      r4.w = dot(r7.xyz, float3(0.298999995,0.587000012,0.114));
      r4.w = sqrt(r4.w);
      r4.w = -r1.y * 0.5 + r4.w;
      r5.x = r6.y ? r4.w : r5.x;
      r6.yw = saturate(r3.yz);
      r7.xyz = t0.Sample(s0_s, r6.yw).xyz;
      r4.w = dot(r7.xyz, float3(0.298999995,0.587000012,0.114));
      r4.w = sqrt(r4.w);
      r4.w = -r1.y * 0.5 + r4.w;
      r5.y = r6.z ? r4.w : r5.y;
      r6.yz = -r1.ww * float2(0.25,0.25) + abs(r5.xy);
      r7.xy = cmp(float2(0,0) < r6.yz);
      r6.yz = cmp(r6.yz < float2(0,0));
      r6.yz = (int2)-r7.xy + (int2)r6.yz;
      r6.yz = (int2)r6.yz;
      r5.zw = max(float2(0,0), r6.yz);
      r4.w = r5.z * r5.w;
      r6.yz = float2(1,1) + -r5.zw;
      r6.yz = cmp(float2(0,0) != r6.yz);
      r7.xy = float2(1.5,1.5) * r0.xy;
      r7.xyzw = r6.yyzz ? r7.xyxy : 0;
      r4.xy = -r7.xy + r4.xy;
      r3.yz = r7.zw + r3.yz;
      r4.w = cmp(0 != r4.w);
      r6.x = r4.w ? 7 : 8;
    } else {
      r4.w = -1;
    }
    r4.w = ~(int)r4.w;
    r3.w = (int)r3.w & (int)r4.w;
    r4.w = cmp((int)r6.x < (int)r0.z);
    r3.w = r4.w ? r3.w : 0;
    if (r3.w != 0) {
      r6.yz = float2(1,1) + -r5.zw;
      r6.yz = cmp(float2(0,0) != r6.yz);
      r7.xy = saturate(r4.xy);
      r7.xyz = t0.Sample(s0_s, r7.xy).xyz;
      r4.w = dot(r7.xyz, float3(0.298999995,0.587000012,0.114));
      r4.w = sqrt(r4.w);
      r4.w = -r1.y * 0.5 + r4.w;
      r5.x = r6.y ? r4.w : r5.x;
      r6.yw = saturate(r3.yz);
      r7.xyz = t0.Sample(s0_s, r6.yw).xyz;
      r4.w = dot(r7.xyz, float3(0.298999995,0.587000012,0.114));
      r4.w = sqrt(r4.w);
      r4.w = -r1.y * 0.5 + r4.w;
      r5.y = r6.z ? r4.w : r5.y;
      r6.yz = -r1.ww * float2(0.25,0.25) + abs(r5.xy);
      r7.xy = cmp(float2(0,0) < r6.yz);
      r6.yz = cmp(r6.yz < float2(0,0));
      r6.yz = (int2)-r7.xy + (int2)r6.yz;
      r6.yz = (int2)r6.yz;
      r5.zw = max(float2(0,0), r6.yz);
      r4.w = r5.z * r5.w;
      r6.yz = float2(1,1) + -r5.zw;
      r6.yz = cmp(float2(0,0) != r6.yz);
      r7.xy = r0.xy + r0.xy;
      r7.xyzw = r6.yyzz ? r7.xyxy : 0;
      r4.xy = -r7.xy + r4.xy;
      r3.yz = r7.zw + r3.yz;
      r4.w = cmp(0 != r4.w);
      r6.x = r4.w ? 8 : 9;
    } else {
      r4.w = -1;
    }
    r4.w = ~(int)r4.w;
    r3.w = (int)r3.w & (int)r4.w;
    r4.w = cmp((int)r6.x < (int)r0.z);
    r3.w = r4.w ? r3.w : 0;
    if (r3.w != 0) {
      r6.yz = float2(1,1) + -r5.zw;
      r6.yz = cmp(float2(0,0) != r6.yz);
      r7.xy = saturate(r4.xy);
      r7.xyz = t0.Sample(s0_s, r7.xy).xyz;
      r4.w = dot(r7.xyz, float3(0.298999995,0.587000012,0.114));
      r4.w = sqrt(r4.w);
      r4.w = -r1.y * 0.5 + r4.w;
      r5.x = r6.y ? r4.w : r5.x;
      r6.yw = saturate(r3.yz);
      r7.xyz = t0.Sample(s0_s, r6.yw).xyz;
      r4.w = dot(r7.xyz, float3(0.298999995,0.587000012,0.114));
      r4.w = sqrt(r4.w);
      r4.w = -r1.y * 0.5 + r4.w;
      r5.y = r6.z ? r4.w : r5.y;
      r6.yz = -r1.ww * float2(0.25,0.25) + abs(r5.xy);
      r7.xy = cmp(float2(0,0) < r6.yz);
      r6.yz = cmp(r6.yz < float2(0,0));
      r6.yz = (int2)-r7.xy + (int2)r6.yz;
      r6.yz = (int2)r6.yz;
      r5.zw = max(float2(0,0), r6.yz);
      r4.w = r5.z * r5.w;
      r6.yz = float2(1,1) + -r5.zw;
      r6.yz = cmp(float2(0,0) != r6.yz);
      r7.xy = r0.xy + r0.xy;
      r7.xyzw = r6.yyzz ? r7.xyxy : 0;
      r4.xy = -r7.xy + r4.xy;
      r3.yz = r7.zw + r3.yz;
      r4.w = cmp(0 != r4.w);
      r6.x = r4.w ? 9 : 10;
    } else {
      r4.w = -1;
    }
    r4.w = ~(int)r4.w;
    r3.w = (int)r3.w & (int)r4.w;
    r4.w = cmp((int)r6.x < (int)r0.z);
    r3.w = r4.w ? r3.w : 0;
    if (r3.w != 0) {
      r6.yz = float2(1,1) + -r5.zw;
      r6.yz = cmp(float2(0,0) != r6.yz);
      r7.xy = saturate(r4.xy);
      r7.xyz = t0.Sample(s0_s, r7.xy).xyz;
      r4.w = dot(r7.xyz, float3(0.298999995,0.587000012,0.114));
      r4.w = sqrt(r4.w);
      r4.w = -r1.y * 0.5 + r4.w;
      r5.x = r6.y ? r4.w : r5.x;
      r6.yw = saturate(r3.yz);
      r7.xyz = t0.Sample(s0_s, r6.yw).xyz;
      r4.w = dot(r7.xyz, float3(0.298999995,0.587000012,0.114));
      r4.w = sqrt(r4.w);
      r4.w = -r1.y * 0.5 + r4.w;
      r5.y = r6.z ? r4.w : r5.y;
      r6.yz = -r1.ww * float2(0.25,0.25) + abs(r5.xy);
      r7.xy = cmp(float2(0,0) < r6.yz);
      r6.yz = cmp(r6.yz < float2(0,0));
      r6.yz = (int2)-r7.xy + (int2)r6.yz;
      r6.yz = (int2)r6.yz;
      r5.zw = max(float2(0,0), r6.yz);
      r4.w = r5.z * r5.w;
      r6.yz = float2(1,1) + -r5.zw;
      r6.yz = cmp(float2(0,0) != r6.yz);
      r7.xy = r0.xy + r0.xy;
      r7.xyzw = r6.yyzz ? r7.xyxy : 0;
      r4.xy = -r7.xy + r4.xy;
      r3.yz = r7.zw + r3.yz;
      r4.w = cmp(0 != r4.w);
      r6.x = r4.w ? 10 : 11;
    } else {
      r4.w = -1;
    }
    r4.w = ~(int)r4.w;
    r3.w = (int)r3.w & (int)r4.w;
    r0.z = cmp((int)r6.x < (int)r0.z);
    r0.z = r0.z ? r3.w : 0;
    if (r0.z != 0) {
      r5.zw = float2(1,1) + -r5.zw;
      r5.zw = cmp(float2(0,0) != r5.zw);
      r6.xy = saturate(r4.xy);
      r6.xyz = t0.Sample(s0_s, r6.xy).xyz;
      r0.z = dot(r6.xyz, float3(0.298999995,0.587000012,0.114));
      r0.z = sqrt(r0.z);
      r0.z = -r1.y * 0.5 + r0.z;
      r5.x = r5.z ? r0.z : r5.x;
      r6.xy = saturate(r3.yz);
      r6.xyz = t0.Sample(s0_s, r6.xy).xyz;
      r0.z = dot(r6.xyz, float3(0.298999995,0.587000012,0.114));
      r0.z = sqrt(r0.z);
      r0.z = -r1.y * 0.5 + r0.z;
      r5.y = r5.w ? r0.z : r5.y;
      r1.yw = -r1.ww * float2(0.25,0.25) + abs(r5.xy);
      r5.zw = cmp(float2(0,0) < r1.yw);
      r1.yw = cmp(r1.yw < float2(0,0));
      r1.yw = (int2)-r5.zw + (int2)r1.yw;
      r1.yw = (int2)r1.yw;
      r1.yw = max(float2(0,0), r1.yw);
      r1.yw = float2(1,1) + -r1.yw;
      r1.yw = cmp(float2(0,0) != r1.yw);
      r0.xy = r0.xy + r0.xy;
      r6.xyzw = r1.yyww ? r0.xyxy : 0;
      r4.xy = -r6.xy + r4.xy;
      r3.yz = r6.zw + r3.yz;
    }
  }
  r0.xy = v1.xy + -r4.xy;
  r0.x = r1.x ? r0.x : r0.y;
  r0.yz = -v1.xy + r3.yz;
  r0.y = r1.x ? r0.y : r0.z;
  r0.z = cmp(r0.x < r0.y);
  r1.y = min(r0.x, r0.y);
  r0.x = r0.x + r0.y;
  r0.x = -r1.y / r0.x;
  r0.x = 0.5 + r0.x;
  r0.y = cmp(r0.w < r1.z);
  r0.z = r0.z ? r5.x : r5.y;
  r0.z = cmp(r0.z < 0);
  r0.y = cmp((int)r0.y != (int)r0.z);
  r0.x = r0.y ? r0.x : 0;
  r0.y = r2.x + r2.y;
  r0.y = r0.y * 2 + r2.z;
  r0.y = r0.y + r2.w;
  r0.y = r0.y * 0.0833333358 + -r0.w;
  r0.y = saturate(abs(r0.y) / r4.z);
  r0.z = r0.y * -2 + 3;
  r0.y = r0.y * r0.y;
  r0.y = r0.y * r0.z;
  r0.y = r0.y * r0.y;
  r0.y = 0.75 * r0.y;
  r0.x = max(r0.x, r0.y);
  r0.xy = r0.xx * r3.xx + v1.yx;
  r0.zw = v1.xy;
  r0.xy = r1.xx ? r0.zx : r0.yw;
  r0.xyz = t0.Sample(s0_s, r0.xy).xyz;
  o0.xyz = r0.xyz;
  o0.w = 1;
}