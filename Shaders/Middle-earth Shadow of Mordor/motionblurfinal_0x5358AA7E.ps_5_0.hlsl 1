// ---- Created with 3Dmigoto v1.3.16 on Mon Jul 13 01:10:17 2026

cbuffer _Globals : register(b0)
{
  float DefaultHeight : packoffset(c0) = {100};
  float DefaultWidth : packoffset(c0.y) = {100};

  struct
  {
    float2 m_Position;
  } MaterialVertexDef_Rigid : packoffset(c1);


  struct
  {
    float2 m_Position;
    float4 m_Weights;
    float4 m_Indices;
  } MaterialVertexDef_Skeletal : packoffset(c2);

  bool bHalfPrecision : packoffset(c5) = false;
  bool bUsePS3CompilerArgs : packoffset(c5.y) = true;
  float4 k_vScreenBufferSize : packoffset(c6) = {0,0,0,0};
  float4 k_vMotionBlurParams : packoffset(c7);
  float3 k_vScene_ZRange : packoffset(c8);
}

SamplerState sNeighborVelocityBufferSampler_s : register(s0);
SamplerState sNoiseTextureSampler_s : register(s1);
SamplerState sSourceBufferSampler_s : register(s2);
SamplerState sCurFrameMapDiscardSampler_s : register(s3);
SamplerState sDepthMapSampler_s : register(s4);
Texture2D<float4> tNeighborVelocityBuffer : register(t0);
Texture2D<float4> tNoiseTexture : register(t1);
Texture2D<float4> tSourceBuffer : register(t2);
Texture2D<float4> tCurFrameMapDiscard : register(t3);
Texture2D<float4> tDepthMap : register(t4);


// 3Dmigoto declarations
#define cmp -


void main(
  float4 v0 : SV_POSITION0,
  float2 v1 : TEXCOORD0,
  float2 w1 : TEXCOORD1,
  float4 v2 : TEXCOORD2,
  out float4 o0 : SV_Target0)
{
  float4 r0,r1,r2,r3,r4,r5,r6,r7,r8,r9,r10,r11,r12,r13,r14,r15,r16,r17,r18,r19,r20,r21,r22,r23,r24,r25;
  uint4 bitmask, uiDest;
  float4 fDest;

  r0.xy = tNeighborVelocityBuffer.Sample(sNeighborVelocityBufferSampler_s, w1.xy).xy;
  r0.xyzw = float4(-0.5,-0.5,-0.5,-0.5) + r0.xyxy;
  r0.xyzw = float4(32,32,32,32) * r0.xyzw;
  r1.xyzw = tCurFrameMapDiscard.Sample(sCurFrameMapDiscardSampler_s, w1.xy).xyzw;
  r2.x = dot(r0.zw, r0.zw);
  r2.x = sqrt(r2.x);
  r2.x = cmp(r2.x >= 0.5);
  r3.xyzw = k_vScreenBufferSize.xyxy * w1.xyzw;
  r2.yz = float2(0.015625,0.015625) * r3.zw;
  r2.yz = frac(r2.yz);
  r2.y = tNoiseTexture.Sample(sNoiseTextureSampler_s, r2.yz).x;
  r2.y = -0.5 + r2.y;
  r0.xyzw = k_vScreenBufferSize.zwzw * r0.xyzw;
  r4.xyzw = r2.yyyy * float4(0.166666672,0.166666672,0.166666672,0.166666672) + float4(-0.833333313,-0.666666687,-0.5,-0.333333343);
  r5.xyzw = r0.zwzw * r4.xxyy + w1.xyzw;
  r2.zw = tSourceBuffer.Sample(sSourceBufferSampler_s, r5.xy).xy;
  r4.x = tDepthMap.Sample(sDepthMapSampler_s, r5.xy).x;
  r6.xyz = tCurFrameMapDiscard.Sample(sCurFrameMapDiscardSampler_s, r5.xy).xyz;
  r7.xy = tSourceBuffer.Sample(sSourceBufferSampler_s, r5.zw).xy;
  r4.y = tDepthMap.Sample(sDepthMapSampler_s, r5.zw).x;
  r8.xyz = tCurFrameMapDiscard.Sample(sCurFrameMapDiscardSampler_s, r5.zw).xyz;
  r9.xyzw = r0.zwzw * r4.zzww + w1.xyzw;
  r4.zw = tSourceBuffer.Sample(sSourceBufferSampler_s, r9.xy).xy;
  r6.w = tDepthMap.Sample(sDepthMapSampler_s, r9.xy).x;
  r10.xyz = tCurFrameMapDiscard.Sample(sCurFrameMapDiscardSampler_s, r9.xy).xyz;
  r7.zw = tSourceBuffer.Sample(sSourceBufferSampler_s, r9.zw).xy;
  r8.w = tDepthMap.Sample(sDepthMapSampler_s, r9.zw).x;
  r11.xyz = tCurFrameMapDiscard.Sample(sCurFrameMapDiscardSampler_s, r9.zw).xyz;
  r12.xyzw = r2.yyyy * float4(0.166666672,0.166666672,0.166666672,0.166666672) + float4(-0.166666672,0.166666672,0.333333343,0.5);
  r13.xyzw = r0.zwzw * r12.xxyy + w1.xyzw;
  r12.xy = tSourceBuffer.Sample(sSourceBufferSampler_s, r13.xy).xy;
  r10.w = tDepthMap.Sample(sDepthMapSampler_s, r13.xy).x;
  r14.xyz = tCurFrameMapDiscard.Sample(sCurFrameMapDiscardSampler_s, r13.xy).xyz;
  r15.xy = tSourceBuffer.Sample(sSourceBufferSampler_s, r13.zw).xy;
  r11.w = tDepthMap.Sample(sDepthMapSampler_s, r13.zw).x;
  r16.xyz = tCurFrameMapDiscard.Sample(sCurFrameMapDiscardSampler_s, r13.zw).xyz;
  r17.xyzw = r0.zwzw * r12.zzww + w1.xyzw;
  r12.zw = tSourceBuffer.Sample(sSourceBufferSampler_s, r17.xy).xy;
  r14.w = tDepthMap.Sample(sDepthMapSampler_s, r17.xy).x;
  r18.xyz = tCurFrameMapDiscard.Sample(sCurFrameMapDiscardSampler_s, r17.xy).xyz;
  r15.zw = tSourceBuffer.Sample(sSourceBufferSampler_s, r17.zw).xy;
  r16.w = tDepthMap.Sample(sDepthMapSampler_s, r17.zw).x;
  r19.xyz = tCurFrameMapDiscard.Sample(sCurFrameMapDiscardSampler_s, r17.zw).xyz;
  r20.xyzw = r2.yyyy * float4(0.166666672,0.166666672,0.166666672,0.166666672) + float4(0.666666687,0.666666687,0.833333313,0.833333313);
  r0.xyzw = r0.xyzw * r20.xyzw + w1.xyzw;
  r20.xy = tSourceBuffer.Sample(sSourceBufferSampler_s, r0.xy).xy;
  r2.y = tDepthMap.Sample(sDepthMapSampler_s, r0.xy).x;
  r21.xyz = tCurFrameMapDiscard.Sample(sCurFrameMapDiscardSampler_s, r0.xy).xyz;
  r20.zw = tSourceBuffer.Sample(sSourceBufferSampler_s, r0.zw).xy;
  r18.w = tDepthMap.Sample(sDepthMapSampler_s, r0.zw).x;
  r22.xyz = tCurFrameMapDiscard.Sample(sCurFrameMapDiscardSampler_s, r0.zw).xyz;
  if (r2.x != 0) {
    r23.xy = tSourceBuffer.Sample(sSourceBufferSampler_s, w1.xy).xy;
    r23.xy = float2(-0.5,-0.5) + r23.xy;
    r23.xy = float2(32,32) * r23.xy;
    r19.w = dot(r23.xy, r23.xy);
    r19.w = sqrt(r19.w);
    r21.w = max(0.200000003, r19.w);
    r22.w = 1 / r21.w;
    r23.x = tDepthMap.Sample(sDepthMapSampler_s, w1.xy).x;
    r5.xyzw = k_vScreenBufferSize.xyxy * r5.xyzw;
    r24.xyzw = float4(0.5,0.5,0.5,0.5) * r5.xyzw;
    r23.yz = float2(0.5,0.5) * r3.zw;
    r2.zw = float2(-0.5,-0.5) + r2.zw;
    r2.zw = float2(32,32) * r2.zw;
    r23.w = -r23.x + r4.x;
    r23.w = saturate(-r23.w * k_vMotionBlurParams.y + 1);
    r4.x = r23.x + -r4.x;
    r4.x = saturate(-r4.x * k_vMotionBlurParams.y + 1);
    r5.xyzw = r5.xyzw * float4(0.5,0.5,0.5,0.5) + -r23.yzyz;
    r5.x = dot(r5.xy, r5.xy);
    r5.x = sqrt(r5.x);
    r2.z = dot(r2.zw, r2.zw);
    r2.z = sqrt(r2.z);
    r2.w = max(0.200000003, r2.z);
    r2.w = r5.x / r2.w;
    r2.w = 1 + -r2.w;
    r2.w = max(0, r2.w);
    r24.xyzw = r3.zwzw * float4(0.5,0.5,0.5,0.5) + -r24.xyzw;
    r5.y = dot(r24.xy, r24.xy);
    r5.y = sqrt(r5.y);
    r24.x = r5.y / r21.w;
    r24.x = 1 + -r24.x;
    r24.x = max(0, r24.x);
    r24.y = r24.x * r4.x;
    r25.x = 0.0999999642 * r2.z;
    r2.z = -r2.z * 0.949999988 + r5.x;
    r5.x = 1 / r25.x;
    r2.z = saturate(r5.x * r2.z);
    r5.x = r2.z * -2 + 3;
    r2.z = r2.z * r2.z;
    r2.z = -r5.x * r2.z + 1;
    r5.x = 0.0999999642 * r19.w;
    r5.y = -r19.w * 0.949999988 + r5.y;
    r5.x = 1 / r5.x;
    r5.y = saturate(r5.y * r5.x);
    r25.x = r5.y * -2 + 3;
    r5.y = r5.y * r5.y;
    r5.y = -r25.x * r5.y + 1;
    r5.y = r5.y + r5.y;
    r25.x = r2.w * r23.w + r22.w;
    r4.x = r24.x * r4.x + r25.x;
    r4.x = r2.z * r5.y + r4.x;
    r2.w = r2.w * r23.w + r24.y;
    r2.z = r2.z * r5.y + r2.w;
    r6.xyz = r6.xyz * r2.zzz;
    r6.xyz = r1.xyz * r22.www + r6.xyz;
    r2.zw = float2(-0.5,-0.5) + r7.xy;
    r2.zw = float2(32,32) * r2.zw;
    r5.y = -r23.x + r4.y;
    r5.y = saturate(-r5.y * k_vMotionBlurParams.y + 1);
    r4.y = r23.x + -r4.y;
    r4.y = saturate(-r4.y * k_vMotionBlurParams.y + 1);
    r5.z = dot(r5.zw, r5.zw);
    r5.z = sqrt(r5.z);
    r2.z = dot(r2.zw, r2.zw);
    r2.z = sqrt(r2.z);
    r2.w = max(0.200000003, r2.z);
    r2.w = r5.z / r2.w;
    r2.w = 1 + -r2.w;
    r2.w = max(0, r2.w);
    r5.w = dot(r24.zw, r24.zw);
    r5.w = sqrt(r5.w);
    r7.x = r5.w / r21.w;
    r7.x = 1 + -r7.x;
    r7.x = max(0, r7.x);
    r7.y = r7.x * r4.y;
    r22.w = 0.0999999642 * r2.z;
    r2.z = -r2.z * 0.949999988 + r5.z;
    r5.z = 1 / r22.w;
    r2.z = saturate(r5.z * r2.z);
    r5.z = r2.z * -2 + 3;
    r2.z = r2.z * r2.z;
    r2.z = -r5.z * r2.z + 1;
    r5.z = -r19.w * 0.949999988 + r5.w;
    r5.z = saturate(r5.z * r5.x);
    r5.w = r5.z * -2 + 3;
    r5.z = r5.z * r5.z;
    r5.z = -r5.w * r5.z + 1;
    r5.z = r5.z + r5.z;
    r4.x = r2.w * r5.y + r4.x;
    r4.x = r7.x * r4.y + r4.x;
    r4.x = r2.z * r5.z + r4.x;
    r2.w = r2.w * r5.y + r7.y;
    r2.z = r2.z * r5.z + r2.w;
    r5.yzw = r8.xyz * r2.zzz + r6.xyz;
    r9.xyzw = k_vScreenBufferSize.xyxy * r9.xyzw;
    r24.xyzw = float4(0.5,0.5,0.5,0.5) * r9.xyzw;
    r2.zw = float2(-0.5,-0.5) + r4.zw;
    r2.zw = float2(32,32) * r2.zw;
    r4.y = -r23.x + r6.w;
    r4.y = saturate(-r4.y * k_vMotionBlurParams.y + 1);
    r4.z = r23.x + -r6.w;
    r4.z = saturate(-r4.z * k_vMotionBlurParams.y + 1);
    r6.xyzw = r9.xyzw * float4(0.5,0.5,0.5,0.5) + -r23.yzyz;
    r4.w = dot(r6.xy, r6.xy);
    r4.w = sqrt(r4.w);
    r2.z = dot(r2.zw, r2.zw);
    r2.z = sqrt(r2.z);
    r2.w = max(0.200000003, r2.z);
    r2.w = r4.w / r2.w;
    r2.w = 1 + -r2.w;
    r2.w = max(0, r2.w);
    r9.xyzw = r3.zwzw * float4(0.5,0.5,0.5,0.5) + -r24.xyzw;
    r6.x = dot(r9.xy, r9.xy);
    r6.x = sqrt(r6.x);
    r6.y = r6.x / r21.w;
    r6.y = 1 + -r6.y;
    r6.y = max(0, r6.y);
    r7.x = r6.y * r4.z;
    r7.y = 0.0999999642 * r2.z;
    r2.z = -r2.z * 0.949999988 + r4.w;
    r4.w = 1 / r7.y;
    r2.z = saturate(r4.w * r2.z);
    r4.w = r2.z * -2 + 3;
    r2.z = r2.z * r2.z;
    r2.z = -r4.w * r2.z + 1;
    r4.w = -r19.w * 0.949999988 + r6.x;
    r4.w = saturate(r4.w * r5.x);
    r6.x = r4.w * -2 + 3;
    r4.w = r4.w * r4.w;
    r4.w = -r6.x * r4.w + 1;
    r4.w = r4.w + r4.w;
    r4.x = r2.w * r4.y + r4.x;
    r4.x = r6.y * r4.z + r4.x;
    r4.x = r2.z * r4.w + r4.x;
    r2.w = r2.w * r4.y + r7.x;
    r2.z = r2.z * r4.w + r2.w;
    r4.yzw = r10.xyz * r2.zzz + r5.yzw;
    r2.zw = float2(-0.5,-0.5) + r7.zw;
    r2.zw = float2(32,32) * r2.zw;
    r5.y = -r23.x + r8.w;
    r5.y = saturate(-r5.y * k_vMotionBlurParams.y + 1);
    r5.z = r23.x + -r8.w;
    r5.z = saturate(-r5.z * k_vMotionBlurParams.y + 1);
    r5.w = dot(r6.zw, r6.zw);
    r5.w = sqrt(r5.w);
    r2.z = dot(r2.zw, r2.zw);
    r2.z = sqrt(r2.z);
    r2.w = max(0.200000003, r2.z);
    r2.w = r5.w / r2.w;
    r2.w = 1 + -r2.w;
    r2.w = max(0, r2.w);
    r6.x = dot(r9.zw, r9.zw);
    r6.x = sqrt(r6.x);
    r6.y = r6.x / r21.w;
    r6.y = 1 + -r6.y;
    r6.y = max(0, r6.y);
    r6.z = r6.y * r5.z;
    r6.w = 0.0999999642 * r2.z;
    r2.z = -r2.z * 0.949999988 + r5.w;
    r5.w = 1 / r6.w;
    r2.z = saturate(r5.w * r2.z);
    r5.w = r2.z * -2 + 3;
    r2.z = r2.z * r2.z;
    r2.z = -r5.w * r2.z + 1;
    r5.w = -r19.w * 0.949999988 + r6.x;
    r5.w = saturate(r5.w * r5.x);
    r6.x = r5.w * -2 + 3;
    r5.w = r5.w * r5.w;
    r5.w = -r6.x * r5.w + 1;
    r5.w = r5.w + r5.w;
    r4.x = r2.w * r5.y + r4.x;
    r4.x = r6.y * r5.z + r4.x;
    r4.x = r2.z * r5.w + r4.x;
    r2.w = r2.w * r5.y + r6.z;
    r2.z = r2.z * r5.w + r2.w;
    r4.yzw = r11.xyz * r2.zzz + r4.yzw;
    r6.xyzw = k_vScreenBufferSize.xyxy * r13.xyzw;
    r7.xyzw = float4(0.5,0.5,0.5,0.5) * r6.xyzw;
    r2.zw = float2(-0.5,-0.5) + r12.xy;
    r2.zw = float2(32,32) * r2.zw;
    r5.y = -r23.x + r10.w;
    r5.y = saturate(-r5.y * k_vMotionBlurParams.y + 1);
    r5.z = r23.x + -r10.w;
    r5.z = saturate(-r5.z * k_vMotionBlurParams.y + 1);
    r6.xyzw = r6.xyzw * float4(0.5,0.5,0.5,0.5) + -r23.yzyz;
    r5.w = dot(r6.xy, r6.xy);
    r5.w = sqrt(r5.w);
    r2.z = dot(r2.zw, r2.zw);
    r2.z = sqrt(r2.z);
    r2.w = max(0.200000003, r2.z);
    r2.w = r5.w / r2.w;
    r2.w = 1 + -r2.w;
    r2.w = max(0, r2.w);
    r7.xyzw = r3.zwzw * float4(0.5,0.5,0.5,0.5) + -r7.xyzw;
    r6.x = dot(r7.xy, r7.xy);
    r6.x = sqrt(r6.x);
    r6.y = r6.x / r21.w;
    r6.y = 1 + -r6.y;
    r6.y = max(0, r6.y);
    r7.x = r6.y * r5.z;
    r7.y = 0.0999999642 * r2.z;
    r2.z = -r2.z * 0.949999988 + r5.w;
    r5.w = 1 / r7.y;
    r2.z = saturate(r5.w * r2.z);
    r5.w = r2.z * -2 + 3;
    r2.z = r2.z * r2.z;
    r2.z = -r5.w * r2.z + 1;
    r5.w = -r19.w * 0.949999988 + r6.x;
    r5.w = saturate(r5.w * r5.x);
    r6.x = r5.w * -2 + 3;
    r5.w = r5.w * r5.w;
    r5.w = -r6.x * r5.w + 1;
    r5.w = r5.w + r5.w;
    r4.x = r2.w * r5.y + r4.x;
    r4.x = r6.y * r5.z + r4.x;
    r4.x = r2.z * r5.w + r4.x;
    r2.w = r2.w * r5.y + r7.x;
    r2.z = r2.z * r5.w + r2.w;
    r4.yzw = r14.xyz * r2.zzz + r4.yzw;
    r2.zw = float2(-0.5,-0.5) + r15.xy;
    r2.zw = float2(32,32) * r2.zw;
    r5.y = -r23.x + r11.w;
    r5.y = saturate(-r5.y * k_vMotionBlurParams.y + 1);
    r5.z = r23.x + -r11.w;
    r5.z = saturate(-r5.z * k_vMotionBlurParams.y + 1);
    r5.w = dot(r6.zw, r6.zw);
    r5.w = sqrt(r5.w);
    r2.z = dot(r2.zw, r2.zw);
    r2.z = sqrt(r2.z);
    r2.w = max(0.200000003, r2.z);
    r2.w = r5.w / r2.w;
    r2.w = 1 + -r2.w;
    r2.w = max(0, r2.w);
    r6.x = dot(r7.zw, r7.zw);
    r6.x = sqrt(r6.x);
    r6.y = r6.x / r21.w;
    r6.y = 1 + -r6.y;
    r6.y = max(0, r6.y);
    r6.z = r6.y * r5.z;
    r6.w = 0.0999999642 * r2.z;
    r2.z = -r2.z * 0.949999988 + r5.w;
    r5.w = 1 / r6.w;
    r2.z = saturate(r5.w * r2.z);
    r5.w = r2.z * -2 + 3;
    r2.z = r2.z * r2.z;
    r2.z = -r5.w * r2.z + 1;
    r5.w = -r19.w * 0.949999988 + r6.x;
    r5.w = saturate(r5.w * r5.x);
    r6.x = r5.w * -2 + 3;
    r5.w = r5.w * r5.w;
    r5.w = -r6.x * r5.w + 1;
    r5.w = r5.w + r5.w;
    r4.x = r2.w * r5.y + r4.x;
    r4.x = r6.y * r5.z + r4.x;
    r4.x = r2.z * r5.w + r4.x;
    r2.w = r2.w * r5.y + r6.z;
    r2.z = r2.z * r5.w + r2.w;
    r4.yzw = r16.xyz * r2.zzz + r4.yzw;
    r6.xyzw = k_vScreenBufferSize.xyxy * r17.xyzw;
    r7.xyzw = float4(0.5,0.5,0.5,0.5) * r6.xyzw;
    r2.zw = float2(-0.5,-0.5) + r12.zw;
    r2.zw = float2(32,32) * r2.zw;
    r5.y = -r23.x + r14.w;
    r5.y = saturate(-r5.y * k_vMotionBlurParams.y + 1);
    r5.z = r23.x + -r14.w;
    r5.z = saturate(-r5.z * k_vMotionBlurParams.y + 1);
    r6.xyzw = r6.xyzw * float4(0.5,0.5,0.5,0.5) + -r23.yzyz;
    r5.w = dot(r6.xy, r6.xy);
    r5.w = sqrt(r5.w);
    r2.z = dot(r2.zw, r2.zw);
    r2.z = sqrt(r2.z);
    r2.w = max(0.200000003, r2.z);
    r2.w = r5.w / r2.w;
    r2.w = 1 + -r2.w;
    r2.w = max(0, r2.w);
    r7.xyzw = r3.zwzw * float4(0.5,0.5,0.5,0.5) + -r7.xyzw;
    r6.x = dot(r7.xy, r7.xy);
    r6.x = sqrt(r6.x);
    r6.y = r6.x / r21.w;
    r6.y = 1 + -r6.y;
    r6.y = max(0, r6.y);
    r7.x = r6.y * r5.z;
    r7.y = 0.0999999642 * r2.z;
    r2.z = -r2.z * 0.949999988 + r5.w;
    r5.w = 1 / r7.y;
    r2.z = saturate(r5.w * r2.z);
    r5.w = r2.z * -2 + 3;
    r2.z = r2.z * r2.z;
    r2.z = -r5.w * r2.z + 1;
    r5.w = -r19.w * 0.949999988 + r6.x;
    r5.w = saturate(r5.w * r5.x);
    r6.x = r5.w * -2 + 3;
    r5.w = r5.w * r5.w;
    r5.w = -r6.x * r5.w + 1;
    r5.w = r5.w + r5.w;
    r4.x = r2.w * r5.y + r4.x;
    r4.x = r6.y * r5.z + r4.x;
    r4.x = r2.z * r5.w + r4.x;
    r2.w = r2.w * r5.y + r7.x;
    r2.z = r2.z * r5.w + r2.w;
    r4.yzw = r18.xyz * r2.zzz + r4.yzw;
    r2.zw = float2(-0.5,-0.5) + r15.zw;
    r2.zw = float2(32,32) * r2.zw;
    r5.y = -r23.x + r16.w;
    r5.y = saturate(-r5.y * k_vMotionBlurParams.y + 1);
    r5.z = r23.x + -r16.w;
    r5.z = saturate(-r5.z * k_vMotionBlurParams.y + 1);
    r5.w = dot(r6.zw, r6.zw);
    r5.w = sqrt(r5.w);
    r2.z = dot(r2.zw, r2.zw);
    r2.z = sqrt(r2.z);
    r2.w = max(0.200000003, r2.z);
    r2.w = r5.w / r2.w;
    r2.w = 1 + -r2.w;
    r2.w = max(0, r2.w);
    r6.x = dot(r7.zw, r7.zw);
    r6.x = sqrt(r6.x);
    r6.y = r6.x / r21.w;
    r6.y = 1 + -r6.y;
    r6.y = max(0, r6.y);
    r6.z = r6.y * r5.z;
    r6.w = 0.0999999642 * r2.z;
    r2.z = -r2.z * 0.949999988 + r5.w;
    r5.w = 1 / r6.w;
    r2.z = saturate(r5.w * r2.z);
    r5.w = r2.z * -2 + 3;
    r2.z = r2.z * r2.z;
    r2.z = -r5.w * r2.z + 1;
    r5.w = -r19.w * 0.949999988 + r6.x;
    r5.w = saturate(r5.w * r5.x);
    r6.x = r5.w * -2 + 3;
    r5.w = r5.w * r5.w;
    r5.w = -r6.x * r5.w + 1;
    r5.w = r5.w + r5.w;
    r4.x = r2.w * r5.y + r4.x;
    r4.x = r6.y * r5.z + r4.x;
    r4.x = r2.z * r5.w + r4.x;
    r2.w = r2.w * r5.y + r6.z;
    r2.z = r2.z * r5.w + r2.w;
    r4.yzw = r19.xyz * r2.zzz + r4.yzw;
    r0.xyzw = k_vScreenBufferSize.xyxy * r0.xyzw;
    r6.xyzw = float4(0.5,0.5,0.5,0.5) * r0.xyzw;
    r2.zw = float2(-0.5,-0.5) + r20.xy;
    r2.zw = float2(32,32) * r2.zw;
    r5.y = -r23.x + r2.y;
    r5.y = saturate(-r5.y * k_vMotionBlurParams.y + 1);
    r2.y = r23.x + -r2.y;
    r2.y = saturate(-r2.y * k_vMotionBlurParams.y + 1);
    r0.xyzw = r0.xyzw * float4(0.5,0.5,0.5,0.5) + -r23.yzyz;
    r0.x = dot(r0.xy, r0.xy);
    r0.y = dot(r2.zw, r2.zw);
    r0.xy = sqrt(r0.xy);
    r2.z = max(0.200000003, r0.y);
    r2.z = r0.x / r2.z;
    r2.z = 1 + -r2.z;
    r2.z = max(0, r2.z);
    r3.xyzw = r3.xyzw * float4(0.5,0.5,0.5,0.5) + -r6.xyzw;
    r2.w = dot(r3.xy, r3.xy);
    r2.w = sqrt(r2.w);
    r3.x = r2.w / r21.w;
    r3.x = 1 + -r3.x;
    r3.x = max(0, r3.x);
    r3.y = r3.x * r2.y;
    r5.z = 0.0999999642 * r0.y;
    r0.x = -r0.y * 0.949999988 + r0.x;
    r0.y = 1 / r5.z;
    r0.x = saturate(r0.x * r0.y);
    r0.y = r0.x * -2 + 3;
    r0.x = r0.x * r0.x;
    r0.x = -r0.y * r0.x + 1;
    r0.y = -r19.w * 0.949999988 + r2.w;
    r0.y = saturate(r0.y * r5.x);
    r2.w = r0.y * -2 + 3;
    r0.y = r0.y * r0.y;
    r0.y = -r2.w * r0.y + 1;
    r0.y = r0.y + r0.y;
    r2.w = r2.z * r5.y + r4.x;
    r2.y = r3.x * r2.y + r2.w;
    r2.y = r0.x * r0.y + r2.y;
    r2.z = r2.z * r5.y + r3.y;
    r0.x = r0.x * r0.y + r2.z;
    r4.xyz = r21.xyz * r0.xxx + r4.yzw;
    r0.xy = float2(-0.5,-0.5) + r20.zw;
    r0.xy = float2(32,32) * r0.xy;
    r2.z = -r23.x + r18.w;
    r2.z = saturate(-r2.z * k_vMotionBlurParams.y + 1);
    r2.w = r23.x + -r18.w;
    r2.w = saturate(-r2.w * k_vMotionBlurParams.y + 1);
    r0.z = dot(r0.zw, r0.zw);
    r0.x = dot(r0.xy, r0.xy);
    r0.xz = sqrt(r0.xz);
    r0.y = max(0.200000003, r0.x);
    r0.y = r0.z / r0.y;
    r0.y = 1 + -r0.y;
    r0.y = max(0, r0.y);
    r0.w = dot(r3.zw, r3.zw);
    r0.w = sqrt(r0.w);
    r3.x = r0.w / r21.w;
    r3.x = 1 + -r3.x;
    r3.x = max(0, r3.x);
    r3.y = r3.x * r2.w;
    r3.z = 0.0999999642 * r0.x;
    r0.x = -r0.x * 0.949999988 + r0.z;
    r0.z = 1 / r3.z;
    r0.x = saturate(r0.x * r0.z);
    r0.z = r0.x * -2 + 3;
    r0.x = r0.x * r0.x;
    r0.x = -r0.z * r0.x + 1;
    r0.z = -r19.w * 0.949999988 + r0.w;
    r0.z = saturate(r0.z * r5.x);
    r0.w = r0.z * -2 + 3;
    r0.z = r0.z * r0.z;
    r0.z = -r0.w * r0.z + 1;
    r0.z = r0.z + r0.z;
    r0.w = r0.y * r2.z + r2.y;
    r0.w = r3.x * r2.w + r0.w;
    r0.w = r0.x * r0.z + r0.w;
    r0.y = r0.y * r2.z + r3.y;
    r0.x = r0.x * r0.z + r0.y;
    r0.xyz = r22.xyz * r0.xxx + r4.xyz;
    r0.xyz = r0.xyz / r0.www;
  } else {
    r0.xyz = r1.xyz;
  }
  r2.y = tDepthMap.Sample(sDepthMapSampler_s, w1.xy).x;
  r2.z = cmp(0 < r2.y);
  r2.y = k_vScene_ZRange.x / r2.y;
  r2.y = r2.z ? r2.y : k_vScene_ZRange.y;
  r2.z = tDepthMap.Sample(sDepthMapSampler_s, v2.xy).x;
  r2.w = cmp(0 < r2.z);
  r2.z = k_vScene_ZRange.x / r2.z;
  r2.z = r2.w ? r2.z : k_vScene_ZRange.y;
  r2.w = tDepthMap.Sample(sDepthMapSampler_s, v2.zy).x;
  r3.x = cmp(0 < r2.w);
  r2.w = k_vScene_ZRange.x / r2.w;
  r2.w = r3.x ? r2.w : k_vScene_ZRange.y;
  r3.x = tDepthMap.Sample(sDepthMapSampler_s, v2.xw).x;
  r3.y = cmp(0 < r3.x);
  r3.x = k_vScene_ZRange.x / r3.x;
  r3.x = r3.y ? r3.x : k_vScene_ZRange.y;
  r3.y = tDepthMap.Sample(sDepthMapSampler_s, v2.zw).x;
  r3.z = cmp(0 < r3.y);
  r3.y = k_vScene_ZRange.x / r3.y;
  r3.y = r3.z ? r3.y : k_vScene_ZRange.y;
  r2.zw = r3.yx + r2.zw;
  r2.z = -r2.y * 2 + r2.z;
  r2.y = -r2.y * 2 + r2.w;
  r2.y = abs(r2.z) + abs(r2.y);
  r2.y = -r2.y * 10000 + 1;
  r2.y = max(0, r2.y);
  r2.y = r2.y * 4 + 0.125;
  r3.xyz = tCurFrameMapDiscard.Sample(sCurFrameMapDiscardSampler_s, v2.xy).xyz;
  r4.xyz = tCurFrameMapDiscard.Sample(sCurFrameMapDiscardSampler_s, v2.zy).xyz;
  r5.xyz = tCurFrameMapDiscard.Sample(sCurFrameMapDiscardSampler_s, v2.xw).xyz;
  r6.xyz = tCurFrameMapDiscard.Sample(sCurFrameMapDiscardSampler_s, v2.zw).xyz;
  r3.x = dot(r3.xyz, float3(0.298999995,0.587000012,0.114));
  r3.y = dot(r4.xyz, float3(0.298999995,0.587000012,0.114));
  r3.z = dot(r5.xyz, float3(0.298999995,0.587000012,0.114));
  r3.w = dot(r6.xyz, float3(0.298999995,0.587000012,0.114));
  r4.xyzw = r3.xzxy + r3.ywzw;
  r2.z = r4.x + -r4.y;
  r5.xz = -r2.zz;
  r5.yw = r4.zz + -r4.ww;
  r2.w = dot(r3.xyzw, float4(1,1,1,1));
  r2.y = r2.y * r2.w;
  r2.y = 0.25 * r2.y;
  r2.y = max(0.00390625, r2.y);
  r2.z = min(abs(r5.w), abs(r2.z));
  r2.y = r2.z + r2.y;
  r2.y = 1 / r2.y;
  r4.xyzw = r5.xyzw * r2.yyyy;
  r4.xyzw = max(float4(-2,-2,-2,-2), r4.xyzw);
  r4.xyzw = min(float4(2,2,2,2), r4.xyzw);
  r4.xyzw = k_vScreenBufferSize.zwzw * r4.xyzw;
  r5.xyzw = r4.zwzw * float4(-0.166666672,-0.166666672,0.166666672,0.166666672) + w1.xyzw;
  r6.xyzw = tCurFrameMapDiscard.Sample(sCurFrameMapDiscardSampler_s, r5.xy).xyzw;
  r5.xyzw = tCurFrameMapDiscard.Sample(sCurFrameMapDiscardSampler_s, r5.zw).xyzw;
  r4.xyzw = r4.xyzw * float4(-0.5,-0.5,0.5,0.5) + w1.xyzw;
  r7.xyzw = tCurFrameMapDiscard.Sample(sCurFrameMapDiscardSampler_s, r4.xy).xyzw;
  r4.xyzw = tCurFrameMapDiscard.Sample(sCurFrameMapDiscardSampler_s, r4.zw).xyzw;
  if (r2.x != 0) {
    r0.w = r1.w;
    o0.xyzw = r0.xyzw;
  } else {
    r0.x = dot(r1.xyz, float3(0.298999995,0.587000012,0.114));
    r0.yz = min(r3.xz, r3.yw);
    r0.y = min(r0.y, r0.z);
    r0.y = min(r0.x, r0.y);
    r0.zw = max(r3.xz, r3.yw);
    r0.z = max(r0.z, r0.w);
    r0.x = max(r0.x, r0.z);
    r1.xyzw = r6.xyzw + r5.xyzw;
    r2.xyzw = float4(0.5,0.5,0.5,0.5) * r1.xyzw;
    r3.xyzw = r7.xyzw + r4.xyzw;
    r3.xyzw = float4(0.25,0.25,0.25,0.25) * r3.xyzw;
    r1.xyzw = r1.xyzw * float4(0.25,0.25,0.25,0.25) + r3.xyzw;
    r0.z = dot(r1.xyz, float3(0.298999995,0.587000012,0.114));
    r0.xy = cmp(r0.xz < r0.zy);
    r0.x = (int)r0.x | (int)r0.y;
    o0.xyzw = r0.xxxx ? r2.xyzw : r1.xyzw;
  }
  return;
}