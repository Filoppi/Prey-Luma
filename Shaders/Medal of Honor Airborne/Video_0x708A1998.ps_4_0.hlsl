// Bink movie pass as translated by dgVoodoo 2.81.3. Same instruction stream and slot map as 0x1AAC12AD (it
// declares COLOR0 without centroid, which changes nothing here), so the whole pass comes from that file.
#include "Video_0x1AAC12AD.ps_5_0.hlsl"
